//EcmaScript or es6
//var keyword pollute the code
/*
var proName='Laptop';
var age =18;

if(age >= 18){
    var driving = true;
}
console.log(driving); 
//code is polluted because var lets the variable to be accessed outside the scope of function// function scope
//while let and const doesnot allow to access the variable outside the function // block scope
function register(){
    var username = "kumud";
    var password = 'secret';
}
console.log(username);
*/
var product = 'Laptop';
var product = 'Mobile';

console.log(product);